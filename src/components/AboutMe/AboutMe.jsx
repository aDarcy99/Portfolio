import React from "react";
import { Container, Heading, Typography } from "../reusable/reusable";

const AboutMe = (props) => {
  return (
    <Container component="section" id="about-me">
      <Heading>About Me</Heading>
      <Typography>🚧 This section is currently under construction 🚧</Typography>
    </Container>
  );
};

export default AboutMe;
